export interface ColumnProps {
    groupName: string,
    groupEntries: object[],
    onClick: any
}

export interface TaskProps {
    id: number,
    title: string,
    date: string,
    onClick: any
}
